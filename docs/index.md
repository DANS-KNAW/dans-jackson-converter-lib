Description
===========

Library with Jackson converters.

This library provides a set of ready-to-use converters for Jackson. These converters are used on classes that represent JSON objects and are
serialized/deserialized by Jackson, typically used in the configuration classes and DTOs (Data Transfer Objects) of DANS microservices.

The converters are located in the `nl.knaw.dans.convert.jackson` package.
